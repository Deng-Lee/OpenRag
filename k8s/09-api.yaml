apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openrag-api-uploads
  namespace: openrag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: openrag
  labels:
    app: openrag-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: openrag-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openrag-api
  namespace: openrag
  labels:
    app: openrag-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openrag-api
  template:
    metadata:
      labels:
        app: openrag-api
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              set -e
              until nc -z postgres 5432; do echo waiting postgres; sleep 2; done
              until nc -z elasticsearch 9200; do echo waiting elasticsearch; sleep 2; done
              until nc -z milvus 19530; do echo waiting milvus; sleep 2; done
              echo deps ready
      containers:
        - name: api
          image: openrag/api:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: APP_ENV
              value: production
            - name: DEBUG
              value: "false"
            - name: LOG_LEVEL
              value: INFO
            - name: RAG_PROJECT_BASE
              value: /app
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_USER
              value: openrag
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DATABASE
              value: openrag
            - name: STORAGE_TYPE
              value: minio
            - name: STORAGE_IMPL
              value: MINIO
            - name: STORAGE_ENDPOINT
              value: http://172.16.31.63:9000
            - name: STORAGE_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MINIO_ROOT_USER
            - name: STORAGE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MINIO_ROOT_PASSWORD
            - name: STORAGE_BUCKET
              value: rag-kb
            - name: STORAGE_PREFIX
              value: openrag
            - name: STORAGE_PUBLIC_URL
              value: http://172.16.31.63:9000
            - name: MILVUS_HOST
              value: milvus
            - name: MILVUS_PORT
              value: "19530"
            - name: MILVUS_RUNTIME_USER
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MILVUS_RUNTIME_USER
            - name: MILVUS_RUNTIME_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MILVUS_RUNTIME_PASSWORD
            - name: MILVUS_INDEX_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MILVUS_INDEX_ADMIN_USER
            - name: MILVUS_INDEX_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: MILVUS_INDEX_ADMIN_PASSWORD
            - name: ELASTICSEARCH__ENABLED
              value: "true"
            - name: ELASTICSEARCH__HOSTS
              value: http://elasticsearch:9200
            - name: ELASTICSEARCH__VERIFY_CERTS
              value: "false"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: SECRET_KEY
            - name: ALGORITHM
              value: HS256
            - name: ACCESS_TOKEN_EXPIRE_MINUTES
              value: "30"
            - name: MAX_UPLOAD_SIZE
              value: "104857600"
            - name: UPLOAD_DIR
              value: /app/uploads
            - name: CORS_ORIGINS
              value: http://localhost
            - name: PREVIEW_PUBLIC_WEB_BASE_URL
              value: https://openrag.guozhijishu.com
            - name: PREVIEW_TOKEN_TTL_SECONDS
              value: "900"
            - name: PREVIEW_TOKEN_MAX_TTL_SECONDS
              value: "1800"
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openrag-secrets
                  key: OPENAI_API_KEY
                  optional: false
            - name: OPENAI_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: OPENAI_BASE_URL
            - name: EMBEDDING_MODEL
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_MODEL
            - name: EMBEDDING_DIMENSION
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_DIMENSION
            - name: EMBEDDING_REVISION
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_REVISION
            - name: EMBEDDING_MODEL_IDENTITY
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_MODEL_IDENTITY
            - name: EMBEDDING_BATCH_SIZE
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_BATCH_SIZE
            - name: EMBEDDING_TIMEOUT_SECONDS
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_TIMEOUT_SECONDS
            - name: EMBEDDING_MAX_ATTEMPTS
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_MAX_ATTEMPTS
            - name: EMBEDDING_PROBE_INTERVAL_SECONDS
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: EMBEDDING_PROBE_INTERVAL_SECONDS
            - name: L1_NAV_MODEL
              valueFrom:
                configMapKeyRef:
                  name: openrag-llm-config
                  key: L1_NAV_MODEL
          volumeMounts:
            - name: service-conf
              mountPath: /app/conf
              readOnly: true
            - name: uploads
              mountPath: /app/uploads
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "2"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 30
      volumes:
        - name: service-conf
          configMap:
            name: openrag-service-conf
        - name: uploads
          persistentVolumeClaim:
            claimName: openrag-api-uploads
